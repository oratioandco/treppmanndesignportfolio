{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Portfolio Filter Configuration",
  "description": "Maps application/company slugs to curated case study selections with visibility control.",
  "type": "object",
  "required": [
    "slug",
    "case_studies"
  ],
  "properties": {
    "slug": {
      "type": "string",
      "description": "URL parameter value. e.g. 'liveeo' → treppmann.design/work?liveeo",
      "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$"
    },
    "title": {
      "type": "string",
      "description": "Page title for this view. e.g. 'Selected Work'"
    },
    "subtitle": {
      "type": "string",
      "description": "Optional subtitle for the portfolio landing."
    },
    "theme": {
      "type": "string",
      "description": "Optional theme override for this view. e.g. 'dark', 'minimal', 'corporate'",
      "default": "default"
    },
    "case_studies": {
      "type": "array",
      "items": {
        "type": "object",
        "required": [
          "id",
          "visibility"
        ],
        "properties": {
          "id": {
            "type": "string",
            "description": "Case study ID matching the case study JSON file."
          },
          "visibility": {
            "type": "string",
            "enum": [
              "featured",
              "browseable",
              "hidden"
            ],
            "description": "featured = shown immediately. browseable = behind 'show more'. hidden = only with ?all parameter."
          },
          "order": {
            "type": "integer",
            "description": "Display order. Lower = first."
          },
          "format_override": {
            "type": "string",
            "enum": [
              "in-depth",
              "executive",
              "blog",
              "process",
              "narrative"
            ],
            "description": "Force a specific format for this case study in this view."
          }
        }
      }
    },
    "show_all_label": {
      "type": "string",
      "default": "Show more work",
      "description": "Label for the toggle that reveals browseable case studies."
    },
    "cv": {
      "type": "object",
      "description": "Tailored CV for this application, linked from the portfolio view.",
      "required": [
        "href"
      ],
      "properties": {
        "href": {
          "type": "string",
          "description": "Path to the PDF, e.g. /documents/tobias-treppmann-cv-kleinanzeigen.pdf"
        },
        "label": {
          "type": "string",
          "description": "Link text. Defaults to 'Download CV (PDF)'."
        },
        "note": {
          "type": "string",
          "description": "Optional short qualifier shown next to the link."
        }
      }
    },
    "bio": {
      "type": "object",
      "description": "Bio written for this application. Overrides the generic first-paint bio in BioRegenerator.",
      "properties": {
        "short": {
          "type": "string"
        },
        "long": {
          "type": "string"
        },
        "context": {
          "type": "object",
          "description": "Sent to the bio API so regeneration stays on-message for this application. Ignored gracefully if the API does not support it.",
          "properties": {
            "company": {
              "type": "string"
            },
            "role": {
              "type": "string"
            },
            "emphasis": {
              "type": "string",
              "description": "What to foreground."
            },
            "avoid": {
              "type": "string",
              "description": "Framings that work against this application."
            }
          }
        }
      }
    }
  }
}
